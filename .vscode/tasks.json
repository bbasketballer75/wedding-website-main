{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Run Jest tests (all features)",
      "command": "npx jest --coverage --runInBand",
      "group": "test"
      // Removed invalid problemMatcher
    }
  ]
}
